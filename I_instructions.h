#include <iostream>
#include <cmath>
#include <string>
#include <fstream>
#include <vector>
#include <algorithm>

using namespace std;
// addi, lw, sw, beq, bne

class I_instructions
{
private:
public:
    void addi();
    void lw();
    void sw();
    void beq();
    void bne();
};